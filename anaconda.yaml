anaconda_config_version: 1
upstream_sources:
  - github:
      identifier: postgres/postgres
      use_max_tag: true
      include_regex: ^REL_\d+_\d+$
      from_pattern: ^REL_(\d+)_(\d+)$
      to_pattern: \1.\2
    packages:
      - postgresql
      - libpq
